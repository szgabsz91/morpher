description = 'Finite State Transducer (FST) based transformation engine implementation'

apply from: "$rootDir/gradle/test.gradle"
apply from: "$rootDir/gradle/protobuf.gradle"

ext {
    displayName = 'Morpher Transformation Engine FST'
    moduleName = 'com.github.szgabsz91.morpher.transformationengines.fst'

    testCoverageLimits = [
        instruction: 95.79,
        line: 95.85,
        branch: 100.0,
        class: 100.0
    ]
}

dependencies {
    compile project(':morpher-core')
    compile project(':morpher-transformation-engine-api')

    compile("org.apache.lucene:lucene-core:${luceneCoreVersion}")
    compile("org.slf4j:slf4j-api:${slf4jVersion}")
    
    runtime("ch.qos.logback:logback-core:${logbackVersion}")
    runtime("ch.qos.logback:logback-classic:${logbackVersion}")
}

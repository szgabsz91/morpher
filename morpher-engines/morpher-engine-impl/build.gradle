description = 'Morpher Engine Impl that can inflect/analyze arbitrary words using multiple affix types, and learn incrementally'

apply from: "$rootDir/gradle/test.gradle"
apply from: "$rootDir/gradle/protobuf.gradle"

ext {
    displayName = 'Morpher Engine Impl'
    moduleName = 'com.github.szgabsz91.morpher.engines.impl'
    mockingEnabled = true
    testCoverageLimits = [
        instruction: 97.35,
        line: 97.78,
        branch: 93.08,
        class: 100.0
    ]
}

dependencies {
    compile project(':morpher-core')
    compile project(':morpher-engine-api')
    compile project(':morpher-transformation-engine-api')
    compile project(':morpher-language-handler-api')

    compile("org.slf4j:slf4j-api:${slf4jVersion}")

    runtime("ch.qos.logback:logback-core:${logbackVersion}")
    runtime("ch.qos.logback:logback-classic:${logbackVersion}")

    testCompile project(':morpher-transformation-engine-astra')
    testCompile project(':morpher-language-handler-hunmorph')

    testCompile("org.apache.commons:commons-text:${commonsTextVersion}")
}
